services:
  hotel-rag:
    build: .
    container_name: hotel-rag
    ports:
      - "8801:8000"
    env_file:
      - .env
    volumes:
      - ./:/app
      - hf_cache:/root/.cache/huggingface
    restart: unless-stopped

volumes:
  hf_cache:
